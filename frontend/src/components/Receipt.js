import React from 'react';

const Receipt = ({ receipt }) => {
    return (
        <div>
            <h2>Receipt</h2>
            {/* Display receipt details */}
        </div>
    );
};

export default Receipt;
